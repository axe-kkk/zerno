# 🌾 Система обліку зерна

Сучасна веб-система для обліку зерна з Web3 інтеграцією.

## 🚀 Швидкий старт

### Варіант 1: Запуск через Docker Compose (рекомендовано)

1. **Переконайтеся, що у вас встановлено Docker та Docker Compose**

2. **Клонуйте репозиторій або перейдіть до папки проекту**

3. **Запустіть всі сервіси однією командою:**
   ```bash
   docker-compose up -d
   ```

4. **Відкрийте браузер та перейдіть за адресою:**
   ```
   http://localhost
   ```

5. **Вхід в систему:**
   - Ім'я користувача: `admin`
   - Пароль: `admin123`

6. **Зупинка сервісів:**
   ```bash
   docker-compose down
   ```

7. **Зупинка з видаленням даних:**
   ```bash
   docker-compose down -v
   ```

### Варіант 2: Локальний запуск (без Docker)

#### Передумови:
- Python 3.11+
- PostgreSQL 15+
- Node.js (опціонально, для статичного сервера)

#### Крок 1: Налаштування бази даних

Створіть базу даних PostgreSQL:
```sql
CREATE DATABASE zerno_db;
CREATE USER zerno_user WITH PASSWORD 'zerno_password';
GRANT ALL PRIVILEGES ON DATABASE zerno_db TO zerno_user;
```

#### Крок 2: Налаштування змінних оточення

Створіть файл `.env` на основі `.env.example`:
```bash
cp .env.example .env
```

Відредагуйте `.env` з вашими налаштуваннями:
```env
DATABASE_URL=postgresql://zerno_user:zerno_password@localhost:5432/zerno_db
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_FULL_NAME=Super Admin
JWT_SECRET_KEY=your-secret-key-here
```

#### Крок 3: Встановлення залежностей

```bash
pip install -r requirements.txt
```

#### Крок 4: Запуск backend

```bash
python run.py
```

Backend буде доступний за адресою: `http://localhost:8000`

#### Крок 5: Запуск frontend

Відкрийте `frontend/login.html` в браузері або використайте простий HTTP сервер:

**Python:**
```bash
cd frontend
python -m http.server 8080
```

**Node.js:**
```bash
cd frontend
npx http-server -p 8080
```

Frontend буде доступний за адресою: `http://localhost:8080`

**Важливо:** При локальному запуску потрібно змінити `API_BASE_URL` в `frontend/static/js/auth.js` та `frontend/static/js/dashboard.js` на `http://localhost:8000/api`

## 📁 Структура проекту

```
zerno/
├── backend/              # Backend (FastAPI)
│   ├── api/             # API роутери
│   ├── models.py        # SQLModel моделі
│   ├── schemas.py       # Pydantic схеми
│   ├── auth.py          # JWT аутентифікація
│   ├── database.py      # Підключення до БД
│   ├── config.py        # Конфігурація
│   └── main.py          # Точка входу
├── frontend/            # Frontend (HTML/CSS/JS)
│   ├── login.html       # Сторінка входу
│   ├── dashboard.html   # Головний дашборд
│   └── static/          # Статичні файли
├── docker-compose.yml   # Docker Compose конфігурація
├── Dockerfile           # Docker образ для backend
├── requirements.txt     # Python залежності
└── README.md           # Документація
```

## 🔑 API Endpoints

### Аутентифікація
- `POST /api/auth/login` - Вхід в систему
- `POST /api/auth/logout` - Вихід з системи

### Користувачі
- `GET /api/users/me` - Інформація про поточного користувача
- `GET /api/users/` - Список користувачів (потрібна авторизація)
- `POST /api/users/` - Створення користувача (тільки супер адмін)
- `PATCH /api/users/{id}` - Оновлення користувача (тільки супер адмін)
- `DELETE /api/users/{id}` - Видалення користувача (тільки супер адмін)

### Каса
- `GET /api/cash/balance` - Отримати баланси каси
- `POST /api/cash/update-balance` - Змінити баланс (тільки супер адмін)
- `GET /api/cash/transactions` - Історія транзакцій (тільки супер адмін)

## 🛠 Технології

### Backend
- **FastAPI** - сучасний веб-фреймворк
- **SQLModel** - ORM на основі SQLAlchemy та Pydantic
- **PostgreSQL** - база даних
- **JWT** - аутентифікація через токени

### Frontend
- **HTML5/CSS3** - структура та стилі
- **JavaScript (ES6+)** - логіка та API інтеграція
- **Nginx** - веб-сервер (в Docker)

## 📝 Примітки

- За замовчуванням створюється супер адмін з даними з `.env`
- JWT токени дійсні 24 години
- База даних автоматично ініціалізується при першому запуску
- Каса створюється автоматично з нульовими балансами

## 🔒 Безпека

**Важливо для продакшену:**
- Змініть `SECRET_KEY` та `JWT_SECRET_KEY` на безпечні значення
- Змініть пароль адміна
- Налаштуйте HTTPS
- Обмежте CORS налаштування
- Використовуйте сильні паролі для бази даних

## 📄 Ліцензія

MIT
